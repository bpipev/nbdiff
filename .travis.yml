language: node_js
node_js:
  - "0.10"
notifications:
  email:
    - "bsplay@gmail.com"
before_script:
    - "python app.py"
env:
    - FLASK=0.9
install:
    - sudo pip install flask==$FLASK --use-mirrors
    - sudo pip install -e . --use-mirrors